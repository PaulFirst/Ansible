- hosts: yandex

  tasks:
    - name: install git
      ansible.builtin.apt:
        name: git
        state: present
        update_cache: no
      become: yes
      tags: git

    - name: update cache
      ansible.builtin.apt:
        update_cache: yes
      become: yes
      tags: cache
